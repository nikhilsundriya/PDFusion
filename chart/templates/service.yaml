apiVersion: v1
kind: Service
metadata:
  name: {{ include "PDFusion.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "PDFusion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "PDFusion.selectorLabels" . | nindent 4 }}
